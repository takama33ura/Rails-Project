<%= form_for(task) do |f| %>
  <% if task.errors.any? %>
<%= form_for(status) do |f| %>
  <% if status.errors.any? %>
    <div id="error_explanation">
      <ul>
        <% task.errors.full_tasks.each do |task| %>
          <li><%= task %></li>
        <% task.errors.full_status.each do |status| %>
          <li><%= status %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>
  
  <%= f.label :content, 'ステータス' %>
  <%= f.text_field :contet %>


  <%= f.submit '投稿' %>
<% end %>
